export const map = (arr, fn) => arr.map(fn);
export const reduce = (arr, init, fn) => arr.reduce(fn, init);
export const filter = (arr, fn) => arr.reduce(fn);

export function abc(){}

export class Xpto{
	constructor(){

	}
}

export default { map, reduce, filter }